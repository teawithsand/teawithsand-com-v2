import { ExplainedError } from "@app/util/explain/error";

export const userMustLogInError: ExplainedError = {
    debugMessage: "user must be logged in",
    error: null,
    messageKey: "user.error.must_log_in",
    messageParams: {},
}